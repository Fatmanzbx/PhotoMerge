"""PhotoMerge — merge N photo sources into one library.

A photo's best-pixels copy and its best-metadata copy are frequently not the
same file.  PhotoMerge catalogs every copy, clusters them by content, and keeps
the best pixels together with the union of the metadata.

See PLAN.md for the design and README.md for the runbook.
"""

__version__ = "0.1.0.dev0"
